const hubby = 'Omor Sani';

// Template String
const myNotes = `I am mojnu ${hubby} . I don't have a Laili.`;
console.log(myNotes);

function maxNumber(array) {
    const max = Math.max(...array);
    return max;
} 

const biggest = maxNumber();